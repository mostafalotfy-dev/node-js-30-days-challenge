<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/forms.css">
    <link rel="stylesheet" href="/css/auth.css">
</head>

<body>
   <%- include('../includes/navigation.ejs') %>

    <main>
        <% if(errors.length > 0) { %>
            <div class="user-message centered">
                <ul>
                    <% for(let error of errors) { %>
                        <li><%= error.msg %></li>
                    <% } %>
                </ul>
            </div>
        <% } %>
        <form class="login-form" action="/login" method="POST">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="form-control">
                <label for="email">E-Mail</label>
                <input type="email" class="<%= validationErrors.find(e=>e.path ==='email') ? 'invalid' : '' %>" name="email" id="email" value="<%= oldInput.email %> ">
            </div>
            <div class="form-control">
                <label for="password">Password</label>
                <input class="<%= validationErrors.find(e=>e.path === 'password') ? 'invalid' : '' %>" type="password" name="password" id="password">
            </div>
            <button class="btn" type="submit">Login</button>
        </form>
          <div class="centered">
            <a href="/reset-password">Reset Password</a>
        </div>
    </main>
<%- include('../includes/end.ejs') %>